namespace Model.Interfaces
{
  /// <summary>
  /// Represents maze objects that can be placed in player bags
  /// </summary>
  /// <remarks>For now it's only keys</remarks>
  public interface IStorable : IPlaceable
  {
  }
}